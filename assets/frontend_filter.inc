(function () {
    'use strict';

    angular.module('app')
    .filter('{$filter}', function() {
        return function(input, parm1, parm2) {
    		var output;
    		return output;
  		}
    });
})();
